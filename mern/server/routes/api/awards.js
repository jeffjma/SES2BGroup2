const express = require("express");
const router = express.Router();
// Load Award Model
const Question = require("../../models/Award");
// Didn't want to add want to add my own api or routing yet but can later if needed.










module.exports = router;